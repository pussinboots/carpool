'use strict';
var app = angular.module('app', ['ngRoute', 'controller', 'service']);
/*app.controller("controller", ['GeoService'], function($scope, GeoService) {});
app.factory('service', ['ngResource'], function ($provide) {});*/